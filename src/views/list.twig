<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Layout basico</title>
</head>
<body>
    <h1>Lista</h1>
    {% for user in users %}
        <h3>{{ user.username|e }}</h3>
        <li>
            Email: {{ user.email|e }}
        </li>
        <li>
            Senha: {{ user.password|e }}
        </li>
    {% endfor %}
</body>
</html>